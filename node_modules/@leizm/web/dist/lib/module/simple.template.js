"use strict";
/**
 * @leizm/web 中间件基础框架 - 内置模块
 * @author Zongmin Lei <leizongmin@gmail.com>
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderFile = exports.render = void 0;
const fs = require("fs");
function render(template, data) {
    return template.replace(/\{\{(.*?)\}\}/g, (match, key) => data[key]);
}
exports.render = render;
function renderFile(fileName, data, callback) {
    fs.readFile(fileName, (err, ret) => {
        if (err)
            return callback(new Error(`SimpleTemplate: renderFile fail: ${err.message}`));
        callback(null, render(ret.toString(), data));
    });
}
exports.renderFile = renderFile;
