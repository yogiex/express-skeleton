/**
 * @leizm/web 中间件基础框架 - 内置中间件
 * @author Zongmin Lei <leizongmin@gmail.com>
 */
import { Context } from "../context";
import { MiddlewareHandle } from "../define";
export interface JsonParserOptions {
    /**
     * 允许的最大body字节，当超过此值时将停止解析，并返回错误
     */
    limit: number;
}
export declare const DEFAULT_JSON_PARSER_OPTIONS: JsonParserOptions;
/**
 * 快速的 JSON Body 解析中间件
 * @param options 选项
 */
export declare function jsonParser(options?: Partial<JsonParserOptions>): MiddlewareHandle<Context>;
