/**
 * @leizm/web 中间件基础框架 - 内置中间件
 * @author Zongmin Lei <leizongmin@gmail.com>
 */
import { Context } from "../context";
import { MiddlewareHandle } from "../define";
export interface CorsOptions {
    /** 允许的域名列表 */
    domain?: string[];
    /** 是否允许任意域名，如果为true则总是允许当前请求来源的域名 */
    any?: boolean;
    /** 增加额外的响应头 */
    headers?: Record<string, string | string[]>;
    /** Access-Control-Max-Age */
    maxAge?: number;
    /** Access-Control-Allow-Credentials */
    credentials?: boolean;
    /** Access-Control-Allow-Headers 允许的请求头 */
    allowHeaders?: string[];
    /** Access-Control-Allow-Methods 允许的请求方法 */
    allowMethods?: string[];
}
export declare const DEFAULT_CORS_OPTIONS: Required<CorsOptions>;
/**
 * CORS中间件
 */
export declare function cors(options?: CorsOptions): MiddlewareHandle<Context>;
